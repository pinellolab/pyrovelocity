name: pyrovelocity-gpu

# generate lock file:
#   conda-lock --conda mamba --log-level DEBUG -f environment-gpu.yml --lockfile conda-lock.yml
# see conda-lock.yml header for installation instructions:
#   conda-lock install -n YOURENV --file conda-lock.yml

channels:
  - conda-forge
  - bioconda
  # https://stackoverflow.com/a/71110028/446907
  # We want to have a reproducible setup, so we don't want default channels,
  # which may be different for different users. All required channels should
  # be listed explicitly here.
  - nodefaults

dependencies:
  - python
  - leidenalg
  - pyro-ppl
  - pip
  - seaborn
  - scvelo
  - scvi-tools
  - pytorch-lightning
  - pytorch-gpu
  - scikit-misc
  - torchmetrics
  - h5py
  - anndata
  - adjusttext
  - astropy
  - click
  - colorlog
  - omegaconf
  - rich
  - hydra-core
  - mlflow
  - termcolor
  # cospar dependencies not implicit above
  # generated by: johnnydep -v 2 cospar
  - ete3
  - fastcluster
  - plotnine
  - matplotlib
  # see: https://github.com/conda-incubator/conda-lock/issues/179
  - pip:
      - cospar
      - hydra-zen

# Non-standard section listing target platforms for conda-lock:
platforms:
  - linux-64
